flask>=3.0.0
requests>=2.31.0
Pillow>=10.0.0
imagehash>=4.3.1
opencv-python>=4.8.0
rich>=13.7.0

# Optional — install manually if needed:
# faster-whisper>=0.10.0   → audio transcription in videos
# pillow-heif>=0.16.0      → HEIC/HEIF photo support (iPhone)
